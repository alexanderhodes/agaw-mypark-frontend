import { StatusTranslationPipe } from './status-translation.pipe';

describe('StatusTranslationPipe', () => {
  it('create an instance', () => {
    const pipe = new StatusTranslationPipe();
    expect(pipe).toBeTruthy();
  });
});
